# Caret review integration: source-bound observation stages

The canonical audit builder now consumes the authenticated original caret
evidence, after all existing classification rules. It does not synthesize a
candidate caret color or change normalization, reference input, renderer code,
plugins, or canonical comparison authoring.

The complete source proof accounts for **118 reviewed groups / 3,154 original
observations**, with **27 groups / 896 observations pending**. These describe
browser-computed versus candidate-local-declaration observation stages. They do
not establish equal computed caret values, visible caret rendering, editable
descendant behavior, or renderer causality. Later chip/tab, range and overlay
reviews are not silently promoted by this bounded integration.

## Production wiring and focused conservation

The builder records the source binding and complete original observations. The
validator reopens and authenticates the original capture before checking exact
classified-row coverage. Invalid or missing bindings, lost/relabeled rows,
changed membership, fabricated completeness and inflated equivalence claims
fail validation. Complete reports require complete original membership; a
focused subset explicitly records every omitted case, input and observation.

The integration test loads the actual prior builder from
`68eaa7d1a573055feed65a0a650d13cd6f19c3d9`, with import relocation as the only
change. Seven normalization functions, alias mapping and value comparison remain
identical. Four original cases exercise a local review beyond its first twelve
display samples, a motion review, and pending slider/tooltip observations.

Both builders retain **390 ordered scalar rows**, including authored examples.
Exactly two caret rows change attribution; the other **388 complete rows** are
identical, SHA-256
`bd7395cda7140d55a1b33a5e88c81330953cea3c4cb526a3a91cd93b744bc41f`.
The two pending observations remain unresolved. The subset records **4,046
missing caret observations**, rather than claiming complete coverage.

An initial focused pass exposed stale gap dependency receipts in child-command
diagnostics. That pass alone was insufficient: the test now additionally
requires all three earlier gap bindings to be valid in both builders, preventing
a shared stale dependency from manufacturing an unrelated-row conservation pass.

## Existing gap evidence preserved

All seven original generators and their no-write `--check` commands were run:

```powershell
node scripts/audit-material-owner-gap-inputs.mjs
node scripts/audit-material-owner-gap-canonical-join.mjs
node scripts/audit-material-explicit-gap-composition.mjs
node scripts/bind-material-explicit-gap-composition.mjs
node scripts/audit-material-gap-motion-requests.mjs
node scripts/audit-material-gap-scalar-rule-loss.mjs
node scripts/bind-material-gap-review-membership.mjs
```

Each generation and subsequent no-write replay exits **0**. The whole seven
JSON objects, compared against `222ca7f`, differ in exactly **11 dependency hash
fields**. Every other value, observation, membership, proof and conclusion is
unchanged. The complete-object comparison log is
`artifacts/material-parity/field-host-flow-input-audit/caret-seven-receipt-conservation.log`,
SHA-256 `93149c7954609816d94c01ec6c81eefdeb085f330325dc1669a4f45513632fc5`.

## Verification

```powershell
node --test --test-concurrency=1 tests/material-parity/owner-caret-canonical-integration.spec.mjs tests/material-parity/owner-caret-proof-commands.spec.mjs
node --test tests/material-parity/owner-caret-input-evidence.spec.mjs
node --test --test-name-pattern='records source fingerprints and actual visual acceptance fields' tests/material-parity/input-equivalence-audit.spec.mjs
node --test tests/parity/material-audit-harness-inventory.spec.mjs
```

The first command passes **4/4**, exit **0**, **669,222.9544 ms**. It includes six
production-validator rejection controls plus the existing complete-source
coverage/source/subset commands and their 45 rejection controls. The integration
test itself takes **437,919.286 ms**. Log:
`artifacts/material-parity/field-host-flow-input-audit/caret-canonical-integration-focused.log`.
SHA-256: `029833a5c51376265b8008a0f1cada546b82f063bf5776dbd3d214c826e7f338`.

The original survey file passes **5/5** after complete original-generator replay
proved that only its enclosing-module receipt changed. Its immutable parent is
not rewritten. See [the receipt investigation](material-caret-survey-receipt-diagnostic.md).
The source inventory and harness inventory checks also pass **1/1** and **4/4**,
respectively. Discovery now includes **112 files**, retaining all 43 legacy
files; the canonical source inventory contains 260 entries.

## Not yet complete

The checked-in canonical report has **not** been regenerated by this increment:
it still records **8,339 groups / 386,891 observations / 2,278 unresolved
signatures**. Full prior/current canonical conservation, no-write regeneration,
historical integration guards, dependent case-index receipts, the complete
current audit harness and the enforced rendering-parity matrix remain required.
The four-case diagnostic is not full-report conservation or output acceptance.

The broader audit still contains unresolved differences. Neither this integration
nor the passing focused tests establish completion of the user’s audit goal.
