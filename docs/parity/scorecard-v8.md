# Stateful interaction and form parity v8

This phase makes common pointer, keyboard, control, and application-update workflows behave like equivalent HTML applications. Work proceeds one browser behavior at a time: add a deterministic real-input fixture, capture the unsupported baseline, implement the general scene-owned behavior, compare state/event/geometry/text/paint after every action, run the full corpus, document the supported boundary, and commit the increment.

## Completion thresholds

- The harness drives real pointer and keyboard input in the existing browser page and Babylon scene and compares normalized event order, focus, supported control state, geometry, text, and screenshots after every action.
- All three representative applications contain useful interactive workflows with pointer and keyboard coverage; at least one continues through responsive resize and application-data update.
- Stable compatible authored IDs preserve supported control state through reflow/update; removed or incompatible controls blur and clean up.
- Repeated interaction/update/resize/dispose cycles do not grow scene resources, observers, listeners, action registrations, focus/cursor/selection/dropdown meshes, or registries.
- No executable event-handler strings are used by the supported public path.
- Full-corpus median SSIM remains at least `0.98`; every representative viewport remains at least `0.95`; at least `95%` of edges are within `2px`; no unexplained error exceeds `5px`; visible text and line counts match exactly.
- Supported normalized interaction state and event logs match exactly, runtime/lifecycle/disposal reports are clean, all tests and both builds pass, commits are incremental, and the final clean commit passes `npm run parity:check` three consecutive times unchanged.

## Starting baseline

The unchanged Phase 7 commit `32d031a` passes 132 tests, the library build, and the production application build with only the two documented size-budget warnings. Its enforcing parity result is 103 fixtures / 137 renders / three viewports; median SSIM `0.9952`; minimum `0.9509`; `99.9%` of measured edges within `2px`; maximum error `3.9921px`; exact visible text; clean runtime and completion thresholds.

The legacy renderer creates visible controls and contains several root-scoped input/focus/action services, but `AstylarRenderOptions` has no typed application event channel. Input setup also owns global keyboard and scene observers outside the render session, and `DOMElement.onclick` is a string-valued legacy field. These paths are evidence to reconcile, not an accepted public interaction contract.

## Increment log

| Increment | Browser expectation | Baseline | Cause and general solution | Focused result | Full result | Status |
| --- | --- | --- | --- | --- | --- | --- |
| Real pointer harness and typed event foundation | A real primary-pointer activation reports `pointerdown`, `pointerup`, and `click` once, in order, with the authored target, current target, button, pointer type, and current value. Handlers live outside serializable `SiteData`; supported bubbling and cancellation expose familiar DOM concepts. | New `interaction-pointer-click`: static SSIM `0.9980`; live SSIM `0.9816`; every edge within `2px`; exact text and focus/control state. Chromium reported the three-event sequence; Astylar reported no application events, so runtime parity failed. | Added real Playwright pointer/keyboard action driving, per-step interaction reports, exact event/control/focus comparison, a pure typed target/bubble dispatcher, and one scene-owned Babylon pointer runtime. `AstylarRenderOptions.events` accepts handlers keyed by authored ID plus an optional dispatch observer. `pointerenter`, `pointerleave`, `focus`, and `blur` are non-bubbling; the remaining currently declared event types traverse authored ancestors. `preventDefault()` and `stopPropagation()` are recorded; later control increments connect cancellation to each supported default action. The runtime rejects hidden, disabled, or removed targets and releases its observer through render-session cleanup. | Static SSIM `0.9980`; live SSIM `0.9816`; every edge within `2px`; maximum error `0.0498px`; exact text, focus, control state, target payload, and `pointerdown → pointerup → click` event log; runtime clean. Four focused unit tests cover propagation, cancellation, disabled/removed targets, exact activation order, and observer disposal. | 104 fixtures / 139 renders / three viewports; median SSIM `0.9952`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 136 tests and both builds pass. | Accepted |

| Scene-owned focus and Tab navigation | Pointer focus occurs between `pointerdown` and `pointerup`; `Tab` and `Shift+Tab` move in authored tab order, skip disabled controls, and report `keydown`, `blur`, then `focus`; clicking a non-focusable target blurs the control. Focus paint preserves authored control styling. | New `interaction-focus-navigation`: five renders; median SSIM `0.9683`; minimum `0.9679`; every edge within `2px`; exact text and legacy internal focus state. Chromium reported focus, blur, and keydown events, while Astylar's public log contained only pointer events. Input setup also installed an immortal `window` keyboard listener, a scene-wide blur observer, duplicate pick actions, and a hard-coded button hover/focus material that painted the control black. | The per-scene runtime now owns one canvas keyboard listener, builds browser-style positive-`tabindex`/DOM order, applies cancellable focus defaults after `pointerdown`, and routes non-Tab keys to the focused control. Input focus is exposed through a narrow adapter; full rebuild and disposal clean its maps and visual resources. Removed the root-global keyboard listener, extra blur observer, duplicate pick attachment, and automatic hard-coded button hover/pressed color mutations. Focus is painted as a CSS-sized `3px` blue ring at `2px` offset without changing authored control materials. | Five renders; median SSIM `0.9942`; minimum `0.9942`; every edge within `2px`; maximum error `0.0259px`; exact text, control state, focused ID, and cumulative event logs at all four actions; one pointer observer and one keyboard listener; runtime clean. The focused runtime test covers disabled-control skipping, browser-order events, default key routing, and listener disposal. | 105 fixtures / 144 renders / three viewports; median SSIM `0.9952`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 137 tests and both builds pass. | Accepted |

| Text input editing and commit events | Printable keys and Backspace mutate the focused text control at its selection, report `input` immediately after each value mutation, keep value/caret/selection synchronized, and report `change` before `blur` when an edited value is committed. Cancelling `keydown` cancels its default mutation and `input`. | New `interaction-text-editing`: five renders; median SSIM `0.9815`; minimum `0.9814`; every edge within `2px`; maximum error `0.0005px`; exact rendered text and edited values. Astylar already changed the value, but omitted every `input` event and the blur-time `change`; Backspace moved its cursor to index 6 while leaving `selectionStart`/`selectionEnd` at 7. | The scene runtime now snapshots supported control state around each keyboard default action and emits `input` only when value/checked/selected state actually changes. It records the entry value for text-entry controls and emits `change` before `blur` only when that value was edited. The text manager now collapses both selection endpoints to the caret after deletion. Public `keydown.preventDefault()` returns before the internal mutation, so cancellation also suppresses `input`. | Five renders; median SSIM `0.9815`; minimum `0.9814`; every edge within `2px`; maximum error `0.0005px`; exact value, focus, selection, and cumulative event logs at all four actions; runtime clean. A focused runtime test covers mutation-time `input`, cancelled default actions, and `change`-before-`blur`. | 106 fixtures / 149 renders / three viewports; median SSIM `0.9952`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 138 tests and both builds pass. | Accepted |

| Textarea selection and multiline editing | Shift-arrow selection exposes ordered `selectionStart`/`selectionEnd` even when the caret moves left from its anchor; typing replaces the selected range; Enter inserts a preserved newline; subsequent typing continues on the new line; `input` is immediate and edited-value `change` precedes blur. | New `interaction-textarea-selection`: six renders; median SSIM `0.9761`; minimum `0.9756`; every edge within `2px`; maximum error `0.0005px`; edited values matched. The document-level page-selection handler consumed both `ArrowLeft` events before the focused textarea runtime, leaving public key logs and selection state unequal. A trailing newline also produced one Astylar layout line against the browser's two. | Page-text keyboard selection now yields to a focused Astylar text control even though its native event target is the canvas. The text manager keeps anchor/focus internally while exposing ascending DOM-style selection endpoints and synchronizes both internal and public endpoints whenever a selection collapses. Preserved wrapping now retains blank and trailing physical lines, fixing textarea line metrics generally. | Six renders; median SSIM `0.9761`; minimum `0.9747`; every edge within `2px`; maximum error `0.0005px`; exact values, focus, cursor, ordered selection endpoints, line counts, and cumulative event logs through all five actions; runtime clean. Four focused tests cover control/page keyboard ownership, leftward endpoints, selection collapse, and preserved trailing lines. | 107 fixtures / 155 renders / three viewports; median SSIM `0.9951`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 142 tests and both builds pass. | Accepted |

| Checkbox pointer activation | A primary click toggles checked state before `click` listeners run, then emits `input` and `change` with the new state. A cancelled `click` exposes the tentative state to the handler, restores the prior state afterward, and suppresses `input`/`change`. An omitted value has the browser default value `on`. | New `interaction-checkbox-click`: three renders; median SSIM `0.9977`; minimum `0.9965`; every edge within `2px`; maximum error `0.0002px`; exact text. A legacy action toggled before the first `pointerup`, the visible indicator child prevented the second toggle, the value was `false`, and both `input` and `change` were absent, so state and event parity failed after each click. | Removed checkbox toggle actions from the control and its child/label meshes. The scene runtime now asks the input adapter for one reversible activation immediately after `pointerup`, dispatches `click` against the tentative checked state, rolls back a cancelled click, or emits `input` then `change` for a completed mutation. Checkbox state changes flow through one manager method so the indicator and normalized state stay synchronized, and missing values initialize to `on`. | Three renders; median and minimum SSIM `0.9977`; every edge within `2px`; maximum error `0.0002px`; exact default value, checked/focus state, paint, and cumulative event logs for check and uncheck; runtime clean. Focused tests cover adapter mutation/rollback plus event ordering and cancelled-click rollback. | 108 fixtures / 158 renders / three viewports; median SSIM `0.9952`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 144 tests and both builds pass. | Accepted |

| Radio-group pointer activation | Clicking an unchecked radio clears its named-group peer and checks the target before `click`, then emits `input` and `change` only on the newly selected target. Clicking the selected radio again changes nothing and emits no mutation events. Focus moves between group members in normal pointer order. | New `interaction-radio-group`: four renders; median SSIM `0.9872`; minimum `0.9870`; every edge within `2px`; maximum error `0.2989px`; exact text and final group state. Legacy mesh actions selected the target before `pointerup`; `input` and `change` were absent after both real group changes. The repeated selected-radio click correctly caused no state mutation but remained embedded in the unequal cumulative event log. | Removed radio activation actions from the control, indicator, and label meshes. The shared reversible activation adapter snapshots every named-group member, delegates one exclusive selection to the manager, reports whether any member changed, and can restore the entire group if `click` is cancelled. The existing runtime therefore supplies browser-order `click`, `input`, and `change` without emitting mutation events for a selected target. | Four renders; median SSIM `0.9872`; minimum `0.9870`; every edge within `2px`; maximum error `0.2989px`; exact values, exclusive checked state, focus transfer, paint, and cumulative event logs through select/repeat/reselect; runtime clean. A focused adapter test covers whole-group mutation and rollback. | 109 fixtures / 162 renders / three viewports; median SSIM `0.9951`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 145 tests and both builds pass. | Accepted |

| Checkbox Space-key activation | Space on a focused checkbox reports `keydown` and `keyup` while checked state is unchanged, then performs a keyboard-synthesized `click` against the tentative toggled state and emits `input` followed by `change`. Cancelling `keydown`, `keyup`, or `click` suppresses or rolls back the activation at the same boundary as the browser. | New `interaction-checkbox-keyboard`: three renders; median and minimum SSIM `0.9977`; every edge within `2px`; maximum error `0.0002px`; exact final state and text. The legacy control handler toggled during `keydown`, so final paint happened to match, but Astylar omitted `keyup`, synthesized `click`, and `change`, and placed `input` at the wrong stage. | Added `keyup` to the typed public event vocabulary and a scene-owned canvas keyup listener paired with keydown. Choice-control Space activation is armed only by an accepted keydown, remains unchanged through keyup dispatch, then reuses the reversible click activation path. Keyboard clicks expose the browser's `button: 0` and empty pointer type. Both listeners and pending activation state are disposed with the scene. | Three renders; median and minimum SSIM `0.9977`; every edge within `2px`; maximum error `0.0002px`; exact checked/focus state, paint, and cumulative pointer plus `keydown → keyup → click → input → change` log; runtime clean. The runtime test covers normal Space activation, prevented-keydown suppression, click cancellation rollback, and listener disposal. | 110 fixtures / 165 renders / three viewports; median SSIM `0.9951`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 145 tests and both builds pass. | Accepted |

| Radio-group arrow navigation | Left/Up and Right/Down move focus to the previous or next enabled member of a named radio group, wrapping at its ends. Chromium orders the default action as `keydown`, old-member `blur`, new-member `focus` while still unchecked, synthesized `click` after selection, `input`, `change`, then `keyup` on the newly focused radio. | New `interaction-radio-keyboard`: four renders; median SSIM `0.9872`; minimum `0.9870`; every edge within `2px`; maximum error `0.2989px`; exact text. Astylar kept focus and selection on the original radio and emitted only `keydown` and `keyup`, so both arrow steps failed event, focus, and group-state parity. | Added a narrow radio-navigation adapter that derives ordered enabled group peers from the control registry. The scene runtime handles all four arrow keys after cancellable public `keydown`, prevents the native canvas default, transfers focus before activating the new member, and reuses reversible synthesized-click activation. Disabled members are skipped, groups wrap, ungrouped and singleton radios remain unchanged, and `keyup` naturally targets the new focus. | Four renders; median SSIM `0.9872`; minimum `0.9870`; every edge within `2px`; maximum error `0.2989px`; exact checked state, focus, paint, and cumulative event logs through focus, forward navigation, and reverse navigation; runtime clean. Focused service/runtime tests cover wrapping, disabled-member skipping, native default prevention, state mutation, focus transfer, and exact event order. | 111 fixtures / 169 renders / three viewports; median SSIM `0.9951`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 147 tests and both builds pass. | Accepted |

| Closed-select arrow selection | ArrowUp/ArrowDown on a focused closed select move to the previous/next enabled option without wrapping, skip disabled options, update the displayed label during keydown, emit `input` then `change`, and expose the new value to keyup. | New `interaction-select-keyboard`: six renders; median SSIM `0.9952`; minimum `0.9937`; every edge within `2px`; maximum error `0.0247px`; exact initial text, focus, and state. Astylar ignored all three arrow actions because its keyboard handler only navigated an open custom dropdown, leaving the original selection and emitting no mutation events. | Select option navigation now distinguishes closed-control commits from open-popup highlighting. Closed arrows select the next enabled option through the manager's normal display-refresh path; the runtime classifies select keyboard mutations as immediate commits and emits both `input` and `change`, while text-entry controls retain their existing input-now/change-on-blur contract. Public keydown cancellation still returns before either mutation or events. | Six renders; median SSIM `0.9952`; minimum `0.9943`; every edge within `2px`; maximum error `0.0247px`; exact visible label, value, selected index/value, focus, paint, and cumulative event logs through disabled-option skipping, forward movement, and reverse movement; runtime clean. Focused tests cover closed navigation, disabled skipping, dirty state, immediate event ordering, and control classification. | 112 fixtures / 175 renders / three viewports; median SSIM `0.9951`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 150 tests and both builds pass. | Accepted |

| Button keyboard activation | A focused button synthesizes `click` during accepted Enter keydown, before keyup. Space leaves the button unchanged through keydown and keyup, then synthesizes `click`. Both keyboard clicks expose `button: 0` and an empty pointer type; cancelling the relevant key event suppresses the default click. | New `interaction-button-keyboard`: four renders; median and minimum SSIM `0.9974`; every edge within `2px`; maximum error `0.0145px`; exact focus, control state, paint, and pointer-click sequence. Astylar reported keydown/keyup but never exposed the browser-synthesized Enter or Space clicks through the typed event API. | The control adapter now identifies enabled buttons for Enter and Space activation. The scene runtime performs Enter activation immediately after accepted public keydown and reuses its pending-keyup path for Space, dispatching both through the existing synthesized-click helper. Choice controls retain Space-only activation, disabled controls are excluded, and key/click cancellation semantics stay centralized. | Four renders; median and minimum SSIM `0.9974`; every edge within `2px`; maximum error `0.0145px`; exact focus, paint, keyboard click payloads, and cumulative event logs through pointer focus, Enter, and Space; runtime clean. Focused tests cover per-control key eligibility and exact Enter-versus-Space click boundaries. | 113 fixtures / 179 renders / three viewports; median SSIM `0.9952`; minimum `0.9509`; `99.9%` of edges within `2px`; maximum error `3.9921px`; exact text; runtime clean. All 152 tests and both builds pass. | Accepted |

## Supported public interaction boundary so far

- Executable functions remain outside JSON `SiteData` in `AstylarRenderOptions.events.handlers`, keyed by stable authored element ID and event type.
- `events.onEvent` observes one normalized snapshot after each completed dispatch.
- Pointer targeting walks the picked Babylon mesh hierarchy to the authored element ID, so text/indicator child meshes retain their control target.
- Pointer focus and forward/reverse Tab navigation support buttons, inputs, textareas, selects, and anchors with `href`; disabled, hidden, and negative-`tabindex` controls are omitted. Positive `tabindex` values precede normal authored order.
- Focus, blur, keydown, and keyup use the same typed dispatch channel. Cancelling pointerdown prevents its focus default; cancelling Tab prevents navigation; cancelling a control key prevents its default action.
- Text inputs and textareas support printable-key insertion, Backspace/Delete, Home/End and arrow caret movement, ordered DOM-style selection endpoints, selection replacement, immediate `input` after value mutations, and edited-value `change` before blur. Textareas additionally preserve Enter-created blank and trailing lines.
- Checkbox pointer activation applies the tentative checked state before `click`, emits `input` then `change` after an accepted mutation, and restores state without mutation events when `click` is cancelled. Missing checkbox values use the browser default `on`.
- Radio pointer activation applies exclusive named-group state before `click`, emits mutation events only when the selection changes, and restores every group member when `click` is cancelled. Arrow keys move focus and selection among enabled named-group members with wrapping and browser-order synthesized activation events.
- Checkbox and radio Space activation is deferred until after keyup, then follows the same reversible `click`, `input`, and `change` path as pointer activation.
- Closed single-select controls support ArrowUp/ArrowDown selection, disabled-option skipping, immediate display refresh, and browser-order `input` then `change`; the custom expanded-popup path remains separate.
- Enabled buttons support browser-timed Enter and Space synthesized clicks through the typed API; disabled buttons receive neither default activation.
- Each active scene owns one pointer observer and a canvas keydown/keyup listener pair. Renderer rebuilds and disposal clean both listeners, pending activation, input registrations, focus state, cursor timers, and focus-ring meshes.
- The dispatcher supports target identity, current-target identity during propagation, value/checked/selected payloads, pointer/keyboard fields, `preventDefault()`, and `stopPropagation()`.
- `DOMElement.onclick` remains type-compatible but is deprecated and is not executed by the supported event path.

Pointer-driven text selection, expanded-select pointer/keyboard defaults, form submit/reset semantics, dynamic authored pseudo-state styling beyond the focus ring, state preservation, and representative workflows remain active Phase 8 work.
