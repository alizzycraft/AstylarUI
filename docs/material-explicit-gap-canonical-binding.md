# Explicit gap composition: canonical membership binding

## Production integration verified on the complete explicit-gap population

The production audit now collects, classifies and validates the existing
composition evidence. It assigns **16 groups / 1,032 observations** to unequal
application/plugin authoring, only after earlier attribution rules decline the
case. Raw inputs, scalar normalization and earlier precedence are unchanged.
This supersedes the preparation status below; the historical binding still
deliberately identifies the original unresolved canonical baseline.

The integration test executes the actual prior builder from commit
`3abdb781279462cd1ca1a78e8cf2b6cdc618f3b5` and the current builder on all 296
original cases containing the eight owners. All **892 scalar rows** match;
all **876 unrelated complete findings** match, with SHA-256
`e63d60e8df091d84cc501d7c176418f24f8b79b4cb7045e557f1b79199af7690`.
Every newly classified row was previously unresolved; original authored
examples and complete memberships remain unchanged. Removing the source ledger,
an observation or a classified finding is rejected by production validation.

Before wiring the integration, the same test failed at the expected missing
classification assertion (`0 != 16`), exit 1. Its log is
`artifacts/material-parity/field-host-flow-input-audit/explicit-gap-integration-before.log`,
SHA-256 `b03e8c07d5006c1d0d75b5fa62e300ae2594d0ed75425d40cb9eb7751b488409`.

```powershell
node --test --test-concurrency=1 tests/material-parity/explicit-gap-canonical-binding.spec.mjs tests/material-parity/explicit-gap-classification.spec.mjs tests/material-parity/explicit-gap-source-binding.spec.mjs tests/material-parity/explicit-gap-coverage.spec.mjs tests/material-parity/explicit-gap-canonical-integration.spec.mjs
node --test --test-name-pattern='records source fingerprints and actual visual acceptance fields' tests/material-parity/input-equivalence-audit.spec.mjs
```

Results: **9/9 passing**, exit 0, **384,292.9741 ms**, followed by the focused
source-inventory test **1/1 passing**, exit 0, **1,286.5594 ms**. Neither run
skipped, cancelled or marked tests todo. The nine-test log is
`artifacts/material-parity/field-host-flow-input-audit/explicit-gap-integration-focused.log`,
SHA-256 `601222b6c7768635fe650385da54b06b60d8b598e5804b3e4ed40f9f3e19af67`.
The source inventory retains all prior 218 entries and adds eleven explicit-gap
dependencies, each checked by filename and actual digest.

Integration changes the audit module's source fingerprint. Four existing proof
receipts were regenerated with their unchanged scripts; their diffs contain
only dependency hashes, not findings, input trees, scalar values or memberships.
The binding now permits those exact receipt fields to advance, independently
replays the original composition and canonical binding, and checks that eight
normalization/mapping functions are identical to the pinned source. It does not
accept arbitrary report changes or authenticate evidence from a new hash alone.

**Limits:** this verifies the complete explicit-gap population, not the full
unrelated-family audit. Historical integration guards, full canonical
generation/no-write/conservation, the full current audit harness and enforced
parity remain required. The checked-in canonical audit is still the prior
2,330-unresolved snapshot and has not been regenerated by this increment.
No renderer, comparison fixture, reference input or threshold was changed.

The [existing composition review](material-explicit-gap-composition.md) already
classifies the eight owners' unequal spacing/structure inputs. This increment
does not replace or reimplement that review. It binds its complete verified
population to the canonical audit after normal/local-omission gap integration.

The [machine binding](material-explicit-gap-canonical-binding.json) preserves
**16 groups, eight owners, 296 distinct cases and 1,032 property observations**.
All 16 groups are still unresolved in the pinned canonical baseline
`3f55f189708dcf4ab640fc18f797a44f5b39bab6`, whose complete inventory has 8,339
groups and 2,330 unresolved groups. This supplement prepares their existing
authoring attribution for production integration; it does not reduce those
canonical counts yet.

## What is newly verified

- Every complete canonical group has the same hash as the independently saved
  original membership join, not merely the same family/property name or count.
- Every ordered occurrence matches the composition review, the join and the
  original captured scalar input. Original input-tree descriptors and input
  hashes agree in all 1,032 observations.
- Both row and column properties are present for every owner; no owner or case
  is sampled away. Original states and the canonical 12-case display samples
  agree exactly with the full join.
- Raw candidate longhands remain omitted while their authored `gap` shorthand
  remains explicit. The canonical comparison's shorthand expansion is retained
  unchanged. Missing local longhands are not replaced with guessed computed
  defaults or mistaken for absence of an authored spacing request.
- Classification remains `application-plugin-authoring-defect` with the existing
  `reviewed-unequal-spacing-composition` attribution. Computed/used-gap,
  renderer-cause and input-equivalence claims are not added.

The owners are the two button-toggle options, checkbox host, two chip hosts,
chip list, dialog actions and grid-list host. Their original review documents
the substituted nested padding/margins, conditional indicator composition,
wrapping differences and absolute-tile-to-grid replacement. Its source-history
and future core-investigation boundaries remain authoritative; no additional
historical cause is inferred from this membership binding.

## Provenance and regression protection

The loader checks the composition review and independent join against their
committed baseline JSON, validates the original capture and existing source
fingerprints, and binds the complete canonical compressed payload by length and
SHA-256. A bounded parser reads all discrepancy rows, not the later full-tree
payload. This is not a replacement for complete decoded canonical regeneration.

The original composition verifier was rerun unchanged with `--check`: exit **0**,
all 1,032 observations and **40** existing negative controls. It retains full
original tree and descendant-composition replay. The new binding verifier does
not claim to independently repeat that layout proof; it proves exact membership
and conservation between those existing evidence sources and the later baseline.

```powershell
node scripts/audit-material-explicit-gap-composition.mjs --check
node scripts/bind-material-explicit-gap-composition.mjs
node --test tests/material-parity/explicit-gap-canonical-binding.spec.mjs
node scripts/bind-material-explicit-gap-composition.mjs --check
```

Generation and no-write binding replay exit **0**. The two new focused tests pass
**2/2**, no failures/skips/cancellations/todos, **4,331.8743 ms**. Fourteen mutation
controls reject lost groups/occurrences, swapped samples, changed input hashes
and tree descriptors, altered longhand/shorthand provenance, forged equal-input
classification and unjustified used-gap claims. The focused log is
`artifacts/material-parity/field-host-flow-input-audit/explicit-gap-binding-focused.log`.

During this increment a redundant composition implementation was briefly
created before the earlier complete review was located. Those new duplicate
files were removed and the original script restored byte-for-byte in the Git
diff. The existing proof was then rerun rather than superseded. No canonical
fixture, renderer, baseline classification or running harness dependency was
changed by the retained increment.

## Remaining work

### Source replay and independent classification coverage prepared

The new `explicit-gap-source-binding.mjs` collector authenticates the saved
membership proof against commit `6af0ecd30f1a4a0c64639664546f312b20c5addd`, checks
its source and parent-proof fingerprints, and executes the unchanged original
composition verifier with `--check`. That verifier reopens every original tree
and descendant composition, retains all 40 negative controls, and confirms the
canonical files were not changed. The verifier's own source hash is checked
before execution. No second composition implementation was introduced.

The collector reopens the original scalar report, compares the complete selected
inputs, and retains all 2,311 capture identities, 516 selected owners in 296
cases, and 1,032 row/column observations. Its expected population comes from the
fixed proof inventory. Removing a source owner records the two missing property
observations; it cannot shrink the denominator. Complete validation rejects
partial coverage. Diagnostic subsets can be checked explicitly with
`requireComplete: false`, but do not establish complete audit coverage.

`explicit-gap-coverage.mjs` independently reopens the original scalar source and
reconstructs all expected classifications, full ordered membership, state lists
and display samples. It compares against the fixed proof inventory rather than
whatever findings survive in the proposed report. Source validation and complete
classification validation are both required for production integration.

```powershell
node --test --test-concurrency=1 tests/material-parity/explicit-gap-source-binding.spec.mjs tests/material-parity/explicit-gap-coverage.spec.mjs
```

Result: exit **0**, **4/4 tests**, no failures/skips/cancellations/todos,
**41,654.9198 ms**. The source tests reject ten changed scalar/tree/identity
populations and additionally exercise full replay with a deleted observation,
an incomplete-coverage claim, and an omitted owner. Thirteen classification
controls reject removed/relabelled findings, changed values, samples, ordered
memberships, states, equivalence claims, source groups and coverage counts.

The log is
`artifacts/material-parity/field-host-flow-input-audit/explicit-gap-source-coverage-focused.log`,
1,186 bytes, SHA-256
`ef228c56396ea77a91f77dc31fd130c4eac764a406afa37552db61a4ea0763e9`.
The new modules are still independent of the production audit and the live
95-file harness. Canonical integration, precedence/conservation checks and a
complete current harness remain pending; no renderer or fixture input changed.

### Prepared classifier, not yet integrated

`tests/material-parity/explicit-gap-classification.mjs` now consumes the verified
composition binding without reimplementing the original-tree review. Its focused
spec first reproduces the saved binding against the committed composition proof,
original scalar inputs and canonical baseline, then exercises all **16 groups /
eight owners / 296 cases / 1,032 observations** through the classifier.

The classifier retains the reference `normal`, explicit candidate shorthand and
raw omitted longhand separately. Every result carries original tree/input and
composition hashes. It explicitly denies whole-element input equivalence,
computed/used-gap verification, historical motivation and confirmed renderer
causality. The ownership recommendation requires an equivalent-input core layout
reduction before removing the plugin/application substitution, not another gap
adjustment. It is a consumer of independently validated evidence, not a standalone
authenticator or a replacement for the complete composition replay.

```powershell
node --test tests/material-parity/explicit-gap-classification.spec.mjs
```

Result: exit **0**, **2/2 tests**, no failures/skips/cancellations/todos,
**3,512.4672 ms**. Thirty rejection controls cover wrong properties/cases/owners,
missing or duplicated memberships, changed raw input evidence, unsupported
equivalence/cause claims and differing local style stages. The stage controls
rehash the changed scalar input deliberately, ensuring that the explicit-stage
guards reject them independently of the input-hash guard. All 1,032 observations
also reject an unrelated property. The log is
`artifacts/material-parity/field-host-flow-input-audit/explicit-gap-classification-focused.log`.

This is an independent preparation step while the 95-file harness continues.
No file in that run's test/dependency inventory was edited. Production collection,
classification precedence, complete coverage validation, canonical generation
and conservation remain pending. The new classifier is **not wired into the
canonical report**, which still has 2,330 unresolved groups. The later-added spec
must be included in a subsequent complete current harness run.

Integrate this source-bound authoring evidence into the production classifier
with existing classification precedence, exact full-case coverage and original
scalar preservation. Run complete canonical generation/no-write/conservation
checks afterward. Keep the 34 motion-only gap groups and four scalar-layer-loss
groups distinct; this binding is not evidence for either population.

The new independent spec was added after the running 95-file harness took its
inventory, so that run cannot establish its full-suite integration. Full current
harness, enforced parity matrix and remaining audit classifications are still
required. Equal-looking one-row output does not justify retaining unequal
wrapping, gutter, indicator or spacing inputs.
